# 外部规范数据库对接占位说明

这一目录当前**不是主任务区**，仅用于以后对接外部规范数据库/法规库时定义字段、接口和映射口径。

## 当前作用
- 说明审查工作流未来如何接外部规范数据库
- 保留占位模板，避免后续对接时字段失控
- 不代表当前阶段由我方负责建设数据库本体

## 当前保留内容
- `templates/`：若未来需要本地映射时可复用的占位模板
- `data-outbound/`：数据出境方向的对接口径说明占位

## 当前原则
- 只定义接口边界，不展开数据库建设
- 不把这个目录当成现阶段主要研发目标
