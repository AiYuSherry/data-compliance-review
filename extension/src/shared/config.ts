import applicationScenarios from '../../config/application-scenarios.json';
import documentTypes from '../../config/document-types.json';
import evidenceRequirements from '../../config/evidence-requirements.json';
import priorityRules from '../../config/priority-rules.json';
import reviewCheckpoints from '../../config/review-checkpoints.json';
import reviewPaths from '../../config/review-paths.json';
import riskThemes from '../../config/risk-themes.json';

export const documentTypesConfig = documentTypes;
export const reviewPathsConfig = reviewPaths;
export const reviewCheckpointsConfig = reviewCheckpoints;
export const priorityRulesConfig = priorityRules;
export const riskThemesConfig = riskThemes;
export const applicationScenariosConfig = applicationScenarios;
export const evidenceRequirementsConfig = evidenceRequirements;
